#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# 设置正确的 Node.js 环境
export PATH="/root/.nvm/versions/node/v18.12.0/bin:$PATH"
export NODE_VERSION="18.12.0"

# 验证 Node.js 版本
node --version

# 生成版本号
pnpm gen:version

# 自动添加修改的版本文件
git add src/lib/version.ts
git add VERSION.txt

npx --no-install lint-staged
